name: build_kernel_ksu_susfs
on:
  workflow_dispatch:
    inputs:
      ksu_source:
        description: 'KSU source: wshamroukh (stable 32997) / upstream (latest) / resukisu (v4.1.0 SUSFS inline hook)'
        required: true
        default: 'wshamroukh'
        type: choice
        options:
          - wshamroukh
          - upstream
          - resukisu
  push:
    branches: [main]

env:
  KERNEL_BRANCH: android-msm-redbull-4.19-android14-qpr1
  SUBLEVEL: "278"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Free Disk Space
        uses: jlumbroso/free-disk-space@main
        with:
          tool-cache: true
          android: false
          dotnet: true
          haskell: true
          large-packages: true
          docker-images: true
          swap-storage: true

      - name: Install dependencies
        run: |
          sudo apt-get update -y
          sudo apt-get install -y repo git wget curl zip cpio bc bison build-essential flex libncurses-dev libssl-dev

      - name: Cache kernel source
        id: cache-kernel
        uses: actions/cache@v4
        with:
          path: android-kernel
          key: kernel-${{ env.KERNEL_BRANCH }}-v3
          restore-keys: |
            kernel-${{ env.KERNEL_BRANCH }}-

      - name: Sync kernel source
        if: steps.cache-kernel.outputs.cache-hit != 'true'
        run: |
          mkdir android-kernel && cd android-kernel
          repo init -u https://android.googlesource.com/kernel/manifest -b ${{ env.KERNEL_BRANCH }}
          repo sync -c --no-tags -j$(nproc)

      - name: Verify cached source
        if: steps.cache-kernel.outputs.cache-hit == 'true'
        run: |
          echo "=== Cache hit! Skipping repo sync ==="
          ls -la android-kernel/private/msm-google/Makefile 2>/dev/null || {
            echo "Cache invalid, re-syncing..."
            rm -rf android-kernel
            mkdir android-kernel && cd android-kernel
            repo init -u https://android.googlesource.com/kernel/manifest -b ${{ env.KERNEL_BRANCH }}
            repo sync -c --no-tags -j$(nproc)
          }

      - name: Modify SUBLEVEL and remove -dirty suffix
        run: |
          cd android-kernel/private/msm-google
          rm -rf out/ AnyKernel3/ KernelSU-Next/ drivers/kernelsu/
          git checkout -- . 2>/dev/null || true
          find . -name "*.rej" -delete 2>/dev/null || true
          find . -name "*.orig" -delete 2>/dev/null || true
          sed -i 's/SUBLEVEL = .*/SUBLEVEL = ${{ env.SUBLEVEL }}/' Makefile
          # Remove -dirty suffix: setlocalversion appends -dirty when git tree has uncommitted changes
          # Our patches modify the source, so git sees it as dirty. Override by:
          # 1. Setting LOCALVERSION to empty (removes custom suffix)
          # 2. Creating .scmversion file (prevents git describe --dirty)
          echo "" > .scmversion
          echo 'LOCALVERSION=' >> Makefile
          grep SUBLEVEL Makefile
          grep LOCALVERSION Makefile

      - name: Setup KernelSU-Next with SUSFS
        run: |
          cd android-kernel/private/msm-google
          case "${{ inputs.ksu_source }}" in
            upstream)
              KSU_REPO="https://github.com/KernelSU-Next/KernelSU-Next"
              KSU_BRANCH="legacy"
              echo "=== Using upstream KSU-Next legacy branch (latest) ==="
              ;;
            resukisu)
              KSU_REPO="https://github.com/ReSukiSU/ReSukiSU"
              KSU_BRANCH="main"
              echo "=== Using ReSukiSU main branch (v4.1.0, SUSFS inline hook) ==="
              ;;
            *)
              KSU_REPO="https://github.com/wshamroukh/KernelSU-Next-SUSFS-kernelv4.19"
              KSU_BRANCH="legacy-susfs"
              echo "=== Using wshamroukh legacy-susfs branch (stable) ==="
              ;;
          esac
          rm -rf KernelSU-Next
          git clone "$KSU_REPO" -b "$KSU_BRANCH" "KernelSU-Next" --depth=1
          ln -sf "$(realpath --relative-to=drivers "KernelSU-Next/kernel")" drivers/kernelsu
          grep -q "kernelsu" drivers/Makefile || printf '\nobj-$(CONFIG_KSU) += kernelsu/\n' >> drivers/Makefile
          grep -q 'source "drivers/kernelsu/Kconfig"' drivers/Kconfig || sed -i '/endmenu/i\source "drivers/kernelsu/Kconfig"' drivers/Kconfig
          echo "=== KernelSU setup complete ==="
          grep -c "KSU_SUSFS" drivers/kernelsu/Kconfig || echo "SUSFS not found in Kconfig!"

      - name: Disable kprobe in KernelSU-Next (force manual hook) — skip for resukisu
        if: inputs.ksu_source != 'resukisu'
        run: |
          cd android-kernel/private/msm-google/KernelSU-Next/kernel
          find . -type f \( -name '*.c' -o -name '*.h' \) -exec sed -i 's/#ifdef CONFIG_KPROBES/#if defined(CONFIG_KPROBES) \&\& 0/g' {} +
          echo "=== kprobe disabled ==="

      - name: Apply SUSFS v2.2.0 complete patch (JackA1ltman NonGKI_Kernel_Build_2nd)
        shell: bash
        run: |
          cd android-kernel
          rm -rf NonGKI_Build
          git clone --depth=1 https://github.com/JackA1ltman/NonGKI_Kernel_Build_2nd.git -b mainline NonGKI_Build || true
          
          cd private/msm-google
          
          # Apply the complete SUSFS v2.2.0 patch (self-contained: susfs.c + susfs.h + susfs_def.h + all kernel hooks)
          # Remove any stale SUSFS files first (cache may have old versions)
          rm -f fs/susfs.c include/linux/susfs.h include/linux/susfs_def.h
          cp ../../NonGKI_Build/Patches/Patch/susfs_patch_to_4.19.patch .
          patch -p1 --batch --no-backup-if-mismatch < susfs_patch_to_4.19.patch || true
          echo "=== SUSFS v2.2.0 patch applied ==="
          
          # Verify SUSFS files were created
          echo "=== Verify ==="
          ls -la fs/susfs.c include/linux/susfs.h include/linux/susfs_def.h 2>&1
          grep "SUSFS_VERSION" include/linux/susfs.h || echo "WARNING: susfs.h missing!"
          grep "SUSFS_MAGIC" include/linux/susfs_def.h || echo "WARNING: susfs_def.h missing!"
          
          # Verify SUSFS files
          echo "=== SUSFS_VERSION ==="
          grep "SUSFS_VERSION" include/linux/susfs.h || echo "NOT in susfs.h!"
          echo "=== SUSFS_MAGIC ==="
          grep "SUSFS_MAGIC" include/linux/susfs_def.h || echo "NOT in susfs_def.h!"
          echo "=== susfs.c exists ==="
          ls -la fs/susfs.c || echo "susfs.c NOT found!"
          
          # Preemptive compat: fsnotify_add_inode_mark may not exist in 4.19
          bash ../../../patches/fsnotify_compat.sh

      - name: Apply hooks (manual hooks for wshamroukh/upstream, inline hook for resukisu)
        shell: bash
        run: |
          cd android-kernel/private/msm-google
          if [ "${{ inputs.ksu_source }}" = "resukisu" ]; then
            echo "=== Using SUSFS inline hook patches (ReSukiSU) ==="
            cp ../../NonGKI_Build/Patches/susfs_inline_hook_patches.sh .
            chmod +x susfs_inline_hook_patches.sh
            bash susfs_inline_hook_patches.sh
            echo "=== SUSFS inline hooks applied ==="
            grep -n 'ksu_handle' fs/exec.c | head -5
            grep -n 'ksu_handle' fs/open.c | head -5
            grep -n 'ksu_handle' fs/read_write.c | head -5
            grep -n 'ksu_handle' fs/stat.c | head -5
            grep -n 'ksu_handle_sys_reboot' kernel/reboot.c | head -3
          else
            echo "=== Using manual hook patches (wshamroukh/upstream) ==="
            cp ../../../patches/normal_patches.sh .
            chmod +x normal_patches.sh
            bash normal_patches.sh
            python3 ../../../patches/patch_reboot.py kernel/reboot.c
            echo "=== Manual hooks applied ==="
            grep -n 'ksu_handle' fs/exec.c | head -5
            grep -n 'ksu_handle' fs/open.c | head -5
            grep -n 'ksu_handle' fs/read_write.c | head -5
            grep -n 'ksu_handle' fs/stat.c | head -5
            grep -n 'ksu_handle_sys_reboot' kernel/reboot.c | head -3
          fi

      - name: Inject KSU + SUSFS config into defconfig and build.config
        shell: bash
        run: |
          cd android-kernel/private/msm-google
          
          # Method 1: Inject into defconfig (skip for resukisu — build system validates defconfig)
          # For all sources, we rely on build.config's scripts/config injection (Method 2)
          if [ "${{ inputs.ksu_source }}" != "resukisu" ]; then
            DEFCONFIG="arch/arm64/configs/redbull_defconfig"
            if [ -f "$DEFCONFIG" ]; then
              echo "" >> "$DEFCONFIG"
              echo "CONFIG_KSU=y" >> "$DEFCONFIG"
              echo "CONFIG_KSU_MANUAL_HOOK=y" >> "$DEFCONFIG"
              echo "CONFIG_KSU_SUSFS=y" >> "$DEFCONFIG"
              echo "CONFIG_KSU_SUSFS_SUS_PATH=y" >> "$DEFCONFIG"
              echo "CONFIG_KSU_SUSFS_SUS_MOUNT=y" >> "$DEFCONFIG"
              echo "CONFIG_KSU_SUSFS_SUS_KSTAT=y" >> "$DEFCONFIG"
              echo "CONFIG_KSU_SUSFS_TRY_UMOUNT=y" >> "$DEFCONFIG"
              echo "CONFIG_KSU_SUSFS_SPOOF_UNAME=y" >> "$DEFCONFIG"
              echo "CONFIG_KSU_SUSFS_ENABLE_LOG=y" >> "$DEFCONFIG"
              echo "CONFIG_KSU_SUSFS_HIDE_KSU_SUSFS_SYMBOLS=y" >> "$DEFCONFIG"
              echo "CONFIG_KSU_SUSFS_SPOOF_CMDLINE_OR_BOOTCONFIG=y" >> "$DEFCONFIG"
              echo "CONFIG_KSU_SUSFS_OPEN_REDIRECT=y" >> "$DEFCONFIG"
              echo "CONFIG_KSU_SUSFS_SUS_MAP=y" >> "$DEFCONFIG"
              echo "=== Config written to $DEFCONFIG ==="
            else
              echo "WARNING: $DEFCONFIG not found"
              find arch/arm64/configs -name "*redbull*" | head -5
            fi
          else
            echo "=== ReSukiSU: skip defconfig modification (build system validates defconfig) ==="
            echo "=== ReSukiSU: relying on build.config scripts/config injection only ==="
          fi
          
          # Method 2: Also inject into build.config update_config (belt and suspenders)
          CONFIG_FILE="build.config.redbull.vintf"
          if [ -f "$CONFIG_FILE" ]; then
            if [ "${{ inputs.ksu_source }}" = "resukisu" ]; then
              KSU_CONFIG_INJECT="-e KSU --disable KSU_MANUAL_HOOK -e KSU_SUSFS -e KSU_SUSFS_SUS_PATH -e KSU_SUSFS_SUS_MOUNT -e KSU_SUSFS_SUS_KSTAT -e KSU_SUSFS_TRY_UMOUNT -e KSU_SUSFS_SPOOF_UNAME -e KSU_SUSFS_ENABLE_LOG -e KSU_SUSFS_HIDE_KSU_SUSFS_SYMBOLS -e KSU_SUSFS_SPOOF_CMDLINE_OR_BOOTCONFIG -e KSU_SUSFS_OPEN_REDIRECT -e KSU_SUSFS_SUS_MAP -e KSU_MANUAL_HOOK_AUTO_SETUID_HOOK -e KSU_MANUAL_HOOK_AUTO_INITRC_HOOK -e KSU_MANUAL_HOOK_AUTO_INPUT_HOOK"
              echo "=== resukisu: SUSFS only, no MANUAL_HOOK ==="
            else
              KSU_CONFIG_INJECT="-e KSU -e KSU_MANUAL_HOOK -e KSU_SUSFS -e KSU_SUSFS_SUS_PATH -e KSU_SUSFS_SUS_MOUNT -e KSU_SUSFS_SUS_KSTAT -e KSU_SUSFS_TRY_UMOUNT -e KSU_SUSFS_SPOOF_UNAME -e KSU_SUSFS_ENABLE_LOG -e KSU_SUSFS_HIDE_KSU_SUSFS_SYMBOLS -e KSU_SUSFS_SPOOF_CMDLINE_OR_BOOTCONFIG -e KSU_SUSFS_OPEN_REDIRECT -e KSU_SUSFS_SUS_MAP"
              echo "=== wshamroukh/upstream: MANUAL_HOOK + SUSFS ==="
            fi
            # Inject into build.config's update_config function
            sed -i "/olddefconfig)/i\\              \${KERNEL_DIR}/scripts/config --file \${OUT_DIR}/.config ${KSU_CONFIG_INJECT}" "$CONFIG_FILE"
            echo "=== Config injected into build.config ==="
          fi

      - name: Port selinux_hide from FlopKernel (skip for upstream/resukisu - already built-in)
        if: inputs.ksu_source != 'upstream' && inputs.ksu_source != 'resukisu'
        shell: bash
        run: |
          cd android-kernel/private/msm-google/KernelSU-Next/kernel
          KSU_ROOT=/home/runner/work/pixel5-kernel-build/pixel5-kernel-build
          bash $KSU_ROOT/patches/port_selinux_hide.sh
          echo "=== selinux_hide port applied ==="

      - name: Verify built-in selinux_hide (upstream/resukisu only)
        if: inputs.ksu_source == 'upstream' || inputs.ksu_source == 'resukisu'
        shell: bash
        run: |
          cd android-kernel/private/msm-google/KernelSU-Next/kernel
          echo "=== Checking built-in selinux_hide ==="
          ls -la feature/selinux_hide.c feature/selinux_hide.h 2>&1
          grep "selinux_hide" Kbuild
          echo "=== selinux_hide already built-in ==="

      - name: Export SELinux static symbols for ReSukiSU selinux_hide (resukisu only)
        if: inputs.ksu_source == 'resukisu'
        shell: bash
        run: |
          cd android-kernel/private/msm-google
          echo "=== Removing static modifiers from SELinux symbols ==="
          
          # 1. selinuxfs.c: write_op array (note: may have 'const' qualifier)
          sed -i 's/static ssize_t (\*\(const \)\?write_op\[\])/ssize_t (*\1write_op[])/' security/selinux/selinuxfs.c
          # 2. selinuxfs.c: sel_handle_status_ops
          sed -i 's/static const struct file_operations sel_handle_status_ops/const struct file_operations sel_handle_status_ops/' security/selinux/selinuxfs.c
          # 3. selinuxfs.c: sel_mutex
          sed -i 's/static DEFINE_MUTEX(sel_mutex);/DEFINE_MUTEX(sel_mutex);/' security/selinux/selinuxfs.c || true
          
          # 4. status.c: selinux_status_page
          sed -i 's/static struct page \*selinux_status_page/struct page *selinux_status_page/' security/selinux/ss/status.c || true
          # 5. status.c: selinux_status_lock
          sed -i 's/static DEFINE_MUTEX(selinux_status_lock)/DEFINE_MUTEX(selinux_status_lock)/' security/selinux/ss/status.c || true
          
          # 6. services.c: policy_rwlock (may already be done by susfs_inline_hook_patches.sh)
          sed -i 's/static DEFINE_RWLOCK(policy_rwlock);/DEFINE_RWLOCK(policy_rwlock);/' security/selinux/ss/services.c || true
          
          echo "=== Verify symbols are no longer static ==="
          grep -n 'write_op\[\]' security/selinux/selinuxfs.c | head -3 || true
          grep -n 'sel_handle_status_ops' security/selinux/selinuxfs.c | head -3 || true
          grep -n 'sel_mutex' security/selinux/selinuxfs.c | head -3 || true
          grep -n 'selinux_status_page' security/selinux/ss/status.c | head -3 || true
          grep -n 'selinux_status_lock' security/selinux/ss/status.c | head -3 || true
          grep -n 'policy_rwlock' security/selinux/ss/services.c | head -3 || true

      - name: Add ksu_handle_devpts stub (upstream only)
        if: inputs.ksu_source == 'upstream'
        shell: bash
        run: |
          cd android-kernel/private/msm-google/KernelSU-Next/kernel
          SUCOMPAT="feature/sucompat.c"
          if ! grep -q "ksu_handle_devpts" "$SUCOMPAT"; then
            echo "" >> "$SUCOMPAT"
            echo "// stub for SUSFS manual hook compatibility (upstream uses LSM hooks)" >> "$SUCOMPAT"
            echo "int ksu_handle_devpts(struct inode *inode) { return 0; }" >> "$SUCOMPAT"
            echo "=== Added ksu_handle_devpts stub to sucompat.c ==="
          else
            echo "=== ksu_handle_devpts already exists in sucompat.c ==="
          fi

      - name: Patch KSU Kbuild to skip hook check during mrproper (skip for resukisu)
        if: inputs.ksu_source != 'resukisu'
        run: |
          cd android-kernel/private/msm-google
          KSU_KBUILD="drivers/kernelsu/Kbuild"
          if [ -f "$KSU_KBUILD" ]; then
            sed -i 's/$(error -- KernelSU-Next: No hooks were defined, please integrate manual hooks in your kernel!)/$(warning -- KernelSU-Next: Hook check bypassed during clean)/g' "$KSU_KBUILD"
            echo "=== Patched Kbuild ==="
          else
            REAL_KBUILD=$(readlink -f "$KSU_KBUILD")
            sed -i 's/$(error -- KernelSU-Next: No hooks were defined, please integrate manual hooks in your kernel!)/$(warning -- KernelSU-Next: Hook check bypassed during clean)/g' "$REAL_KBUILD"
            echo "=== Patched real Kbuild ==="
          fi

      - name: Build kernel
        run: |
          cd android-kernel
          rm -rf out/ AnyKernel3/
          LTO=thin ./private/msm-google/build_redbull-gki.sh

      - name: Verify build output
        run: |
          echo "=== Build output ==="
          ls -la android-kernel/out/android-msm-pixel-4.19/dist/ 2>/dev/null || echo "dist not found"
          find android-kernel/out/ -name "Image.lz4-dtb" 2>/dev/null
          find android-kernel/out/ -name "*.ko" 2>/dev/null | head -10
          find android-kernel/out/ -name "boot.img" 2>/dev/null

      - name: Setup AnyKernel3
        run: |
          cd android-kernel
          rm -rf AnyKernel3
          git clone --depth=1 https://github.com/osm0sis/AnyKernel3.git
          sed -i 's/do.devicecheck=1/do.devicecheck=0/g' AnyKernel3/anykernel.sh
          sed -i 's!block=/dev/block/platform/omap/omap_hsmmc.0/by-name/boot;!block=auto;!g' AnyKernel3/anykernel.sh
          sed -i 's/is_slot_device=0;/is_slot_device=auto;/g' AnyKernel3/anykernel.sh
          sed -i 's/do.modules=0/do.modules=1/g' AnyKernel3/anykernel.sh
          sed -i 's/do.systemless=1/do.systemless=0/g' AnyKernel3/anykernel.sh
          cp out/android-msm-pixel-4.19/dist/Image.lz4-dtb AnyKernel3/
          mkdir -p AnyKernel3/modules/lib/modules
          find out/android-msm-pixel-4.19/dist/ -name "*.ko" -exec cp {} AnyKernel3/modules/lib/modules/ \;
          cp out/android-msm-pixel-4.19/dist/modules.load AnyKernel3/modules/lib/modules/ 2>/dev/null || true
          cp out/android-msm-pixel-4.19/dist/modules.dep AnyKernel3/modules/lib/modules/ 2>/dev/null || true
          cp out/android-msm-pixel-4.19/dist/modules.alias AnyKernel3/modules/lib/modules/ 2>/dev/null || true
          cp out/android-msm-pixel-4.19/dist/vendor_boot.img AnyKernel3/ 2>/dev/null || true
          cp out/android-msm-pixel-4.19/dist/dtbo.img AnyKernel3/ 2>/dev/null || true
          echo "=== Modules ==="
          find AnyKernel3/modules/ -name "*.ko" | wc -l
          cd AnyKernel3
          zip -r9 ../redfin-ksu-susfs-kernel.zip ./*
          echo "=== AnyKernel3 zip created ==="
          ls -la ../redfin-ksu-susfs-kernel.zip

      - name: Upload AnyKernel3 zip
        uses: actions/upload-artifact@v4
        with:
          name: redfin-ksu-susfs-kernel
          path: android-kernel/redfin-ksu-susfs-kernel.zip

      - name: Upload boot.img
        uses: actions/upload-artifact@v4
        with:
          name: boot.img
          path: android-kernel/out/android-msm-pixel-4.19/dist/boot.img

      - name: Upload vendor_boot.img
        uses: actions/upload-artifact@v4
        with:
          name: vendor_boot.img
          path: android-kernel/out/android-msm-pixel-4.19/dist/vendor_boot.img

      - name: Upload dtbo.img
        uses: actions/upload-artifact@v4
        with:
          name: dtbo.img
          path: android-kernel/out/android-msm-pixel-4.19/dist/dtbo.img

      - name: Upload kernel modules
        uses: actions/upload-artifact@v4
        with:
          name: kernel-modules
          path: android-kernel/out/android-msm-pixel-4.19/dist/*.ko
          if-no-files-found: warn
